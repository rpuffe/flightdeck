#!/bin/sh
# App test command — replace with your real tests; make test runs this.
# App-owned: survives make upgrade.

echo "no tests defined yet — replace test.sh with your app's real test command"
exit 0
