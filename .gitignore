# Local agent instructions — private, never committed
CLAUDE.local.md
.claude/settings.local.json

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!example.tfvars
crash.log
*.tfplan

# OS / editor
.DS_Store

# Transient first-run artifact from make bootstrap/plan-bootstrap
backend.tf.off

# Python bytecode (scaler Lambda source lives in bootstrap/lambda)
__pycache__/
